---
label: Page
hide_body: true
display_field: first_paragraph
fields:
- name: page_image
  type: file
  config:
    maxSize: 64
  label: Page Image
  description: A banner image for the page (optional)
- name: first_paragraph
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: First Paragraph
  description: Use this area to write up the salient points of the page. If you get
    it all done with just the first paragraph, that's fine.
- name: extra_text_content
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: Extra Text Content
  description: Additional content past the first paragraph.
